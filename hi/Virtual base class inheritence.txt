Virtual base class inheritence:

The most derived class is responsible for initializing the virtual base class.

If it does not, then the compiler will call the constructor of the first class that inherited.
